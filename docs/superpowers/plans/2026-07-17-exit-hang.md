# Codex HUD Exit Hang Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Execute inline with focused Vitest regression tests before the full plugin update flow.

**Goal:** Make Codex HUD return immediately when the official Codex process exits.

**Architecture:** Make rollout discovery cancellable so its polling timer cannot retain the Node event loop after Codex exits. Request tmux session cleanup asynchronously so the process does not synchronously wait while destroying its own pane and session.

**Tech Stack:** TypeScript, Node.js child processes, AbortSignal, tmux, pnpm, Vitest.

---

## Task 1: Add cancellable rollout discovery

**Files:**

- Modify: `src/runtime/session-binding.ts`
- Modify: `src/runtime/session-binding.test.ts`

- [ ] Add a test proving an aborted discovery returns promptly.
- [ ] Ensure pending delay timers are cleared when aborted.

## Task 2: Make child shutdown non-blocking

**Files:**

- Modify: `src/runtime/launcher.ts`
- Modify: `src/runtime/launcher.test.ts`

- [ ] Abort long discovery immediately when the Codex child exits.
- [ ] Retain a short final rollout check for very fast child processes.
- [ ] Send tmux cleanup asynchronously and unref it.
- [ ] Verify a child that creates no rollout exits without waiting for the discovery timeout.

## Task 3: Validate and reinstall

**Files:**

- Generated: `dist/`
- Generated: `plugins/codex-hud/runtime/`
- Updated by helper: `plugins/codex-hud/.codex-plugin/plugin.json`

- [ ] Run focused and full tests, lint, typecheck, and build.
- [ ] Update cachebuster, validate, reinstall the plugin, and reinstall managed commands.
